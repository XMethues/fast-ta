---
template_version: 1
date: 2026-07-13T18:22:08+0800
author: unknown
commit: c6f3630
branch: main
repository: fast-ta
topic: "Validation of 实现Volume分组指标"
status: ready
verdict: pass
parent: ".rpiv/artifacts/plans/2026-07-13_15-53-08_volume-indicators.md"
tags: [validation, blueprint, ta-core, volume, ad, obv, adosc]
last_updated: 2026-07-13T18:22:08+0800
---

## Validation Report: 实现Volume分组指标

### Implementation Status

- ✓ Phase 1: AD foundation — Fully implemented
- ✓ Phase 2: OBV — Fully implemented
- ✓ Phase 3: ADOSC — Fully implemented
- ✓ Phase 4: Benchmarks — Fully implemented

### Automated Verification Results

- ✓ Volume integration tests: `cargo test -p ta-core --test volume` — run for Phases 1–3 as written; 17 tests passed on each run.
- ✓ Inventory tests: `cargo test -p ta-core --test inventory` — run for Phases 1–3 as written; 7 tests passed on each run.
- ✓ f32 Volume tests: `cargo test -p ta-core --features f32 --test volume` — run for Phases 1–3 as written; 17 tests passed on each run.
- ✓ Benchmark compile: `cargo bench -p ta-benchmarks --bench first_tranche --no-run` — Criterion target compiled with the Volume group registered.
- ✓ Workspace check: `cargo check --workspace` — all workspace crates checked without errors.
- ✓ Workspace tests: `cargo test --workspace` — 155 tests passed; one documentation example remained intentionally ignored.
- ✓ Full f32 ta-core suite: `cargo test -p ta-core --features f32` — 153 tests passed; one documentation example remained intentionally ignored.
- ✓ Formatting: `cargo fmt --all -- --check` — clean.
- ✓ Diff hygiene: `git diff --check` — clean.
- ✓ No regressions detected.

### Code Review Findings

#### Matches Plan:

- `crates/ta-core/src/volume/mod.rs:6-12` and `crates/ta-core/src/lib.rs:49-50` — private child modules, complete public exports, and root `volume` wiring are present.
- `crates/ta-core/src/volume/ad.rs:40-90` — AD validates HLCV inputs, treats every non-positive range as zero contribution, accumulates from index 0, and returns compact `OutputRange::new(0, len)`.
- `crates/ta-core/src/volume/ad.rs:153-194` — AD implements batch/streaming traits and fully resets cumulative state.
- `crates/ta-core/src/volume/obv.rs:51-73` — OBV implements the approved lookback-1 contract, starts from zero, ignores index-zero volume, and applies signed current volume from index 1.
- `crates/ta-core/src/volume/obv.rs:125-168` — trait lookback, first-tick streaming warm-up, finite validation, and reset match batch semantics.
- `crates/ta-core/src/volume/adosc.rs:41-99` — local EMA state mirrors existing SMA-seeded EMA behavior and enforces nonzero periods with strict `fastperiod < slowperiod`.
- `crates/ta-core/src/volume/adosc.rs:104-164` — ADOSC is a single-pass dual-EMA kernel with no intermediate AD vector and correct compact/padded alignment.
- `crates/ta-core/src/volume/adosc.rs:237-300` — trait, streaming warm-up, and reset behavior align with `slowperiod - 1`.
- `crates/ta-core/tests/volume.rs:24-386` — 17 integration tests cover values, padding, trait surfaces, streaming alignment, reset, non-positive ranges, flat closes, period validation, insufficient input, nonfinite input, and output-buffer errors.
- `crates/ta-core/src/inventory.rs:12,201-204` and `crates/ta-core/tests/inventory.rs:86-88,169-174` — implemented count is 45 and AD/ADOSC/OBV are represented in status and trait checks.
- `crates/ta-benchmarks/benches/first_tranche.rs:360-424,512-522` — all three compact kernels have size-matrix Criterion coverage, prepared outside timed loops, and `bench_volume` is registered.
- Working-tree inspection found no changes under `ta-py`, `ta-wasm`, Cargo manifests, workspace membership, or SIMD backends.

#### Deviations from Plan:

None. Implementation is a faithful realization of the approved plan.

#### Pattern Conformance:

- ✓ Facade exports, uppercase function/struct pairs, named SoA inputs/ticks, fallible constructors, compact `_vec` wrappers, `Indicator`, `StreamingIndicator`, and `Resettable` follow existing Price Transform, Volatility, and Overlap conventions.
- ✓ Validation and output handling consistently reuse shared `Float`, `Result`, `OutputRange`, finite/length validators, `compact_buffer`, and `padded_from_compact`.
- ✓ Criterion setup follows the existing public-API and allocation-exclusion pattern.
- Minor observation: OBV's lookback-1 seed is intentionally different from common TA-Lib index-zero OBV behavior; this is an approved, documented decision rather than a deviation.
- Minor observation: ADOSC locally mirrors EMA state instead of composing the public EMA type, as explicitly selected by the plan; current formulas and reset behavior match the existing EMA implementation.

### Manual Testing Required:

None remaining — all manual criteria were verified by source inspection and automated behavior tests:

1. API and scope:
   - [x] `ta_core::volume` exposes AD, OBV, and ADOSC with all planned wrappers, inputs, ticks, and traits.
   - [x] No adapter, manifest, workspace, or SIMD backend changes were introduced.
2. Indicator behavior:
   - [x] AD non-positive-range handling, lookback 0, streaming accumulation, and reset are aligned.
   - [x] OBV compact/padded/streaming lookback is 1 and reset restores first-tick warm-up.
   - [x] ADOSC strict period ordering, SMA-seeded dual EMA, lookback, streaming, and complete state reset are aligned.
3. Inventory and performance:
   - [x] Inventory count/status and trait assertions cover all three functions.
   - [x] Benchmarks use public APIs, prepared fixtures, reusable buffers, all configured sizes, and Criterion registration.

### Recommendations:

- Ready to commit — implementation is complete and validated.
